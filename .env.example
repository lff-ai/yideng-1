# Environment Variables for Web3 AI Chat Platform
# Copy this file to .env and update with your actual values

# GraphQL API Endpoint
# For local development (requires running Wrangler dev server):
VITE_GRAPHQL_ENDPOINT=http://localhost:8787/graphql

# For production deployment, update to your deployed Worker URL:
# VITE_GRAPHQL_ENDPOINT=https://your-worker.your-subdomain.workers.dev/graphql

# Backend Configuration (Set in Cloudflare Dashboard)
# These should be configured as secrets in Cloudflare Workers, not in this file:
# - OPENAI_API_KEY: Your OpenAI API key for GPT-3.5-turbo integration
